---
name: frontend-design
description: Use when the user explicitly names frontend-design or an older workflow references this compatibility name; resolve to frontend-page-router.
license: Complete terms in LICENSE.txt
---

# Frontend Design — compatibility entry

Use `frontend-page-router` as the single canonical entry. Record that canonical
name in the route decision; do not add this alias as another support.

Choose `frontend-ops-platform` or `frontend-report-page` there. The shared finance
design rules now live in `frontend-page-router/references/finance-design.md`.
An existing page mode may be reused; do not repeat classification or confirmation.

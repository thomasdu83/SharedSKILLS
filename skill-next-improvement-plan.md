# SharedSKILLS 下一轮改进计划

制定日期：2026-09-13  
适用范围：`F:\Thomas\SharedSKILLS`。本计划不直接修改 `F:\Thomas\QuantSystem\.trae`。  
目标模型基线：GPT-5.4 及以上，重点是投资判断正确性、证据可追溯性、研究到组合的闭环和长期可维护性。

## 目标状态

SharedSKILLS 应从“有较好规则的技能集合”升级为“可发布、可回滚、可验证的投资研究操作系统”。一次任务至少要能回答六个问题：

1. 这是什么类型的工作，主 Skill 为什么是它？
2. 输入数据的时间口径、来源和缺口是什么？
3. 哪些内容是事实、管理人口径、推断、判断和行动？
4. 当前结果处于研究、候选、组合输入还是生产阶段？
5. 结果是否真的改变了目标权重、实际交易或风险管理？
6. 这次经验是否值得进入共享 Skill，后续是否证明它有效？

这里不把所有 Skill 强行改成短文。长篇领域知识可以保留，但必须把硬门槛、决策顺序和输出契约置于入口，把背景知识、例外处理和实现细节放入按需加载的 references。这样是为了可维护和可验证，不是为了迁就低能力模型。

## 当前基线与完成判定

当前已经具备：56 个 Skill 的资产盘点、核心注册表、处置台账、统一证据/生命周期/产物契约、ODD 四态判定、定量报告边界规则、复盘七态生命周期和组合反馈最小契约。静态检查通过，但行为评估尚未执行，且全库路由、版本锁定和真实投资反馈仍不完整。

本轮不以“所有 SKILL.md 都改过”为完成标准，而以以下结果为标准：

- 每个 Skill 都明确是主入口、辅助能力、参考能力或元能力，不再出现“未登记但也不说明原因”。
- 契约状态的含义互不重叠，测试器能发现非法状态、缺字段和跨契约偷换。
- GPT-5.4 及以上模型在真实历史任务和压力案例中，能稳定保留关键门槛，不能伪造证据、自动晋级生产或错误回写事实层。
- 研究报告、网页、文档和表格均消费同一个冻结研究产物，不在交付阶段重新改变投资结论。
- 研究结论可以追踪到组合提案、实际持仓、成交、成本、PnL、归因和暂停/退役决策；缺失时明确停在待复核状态。
- SharedSKILLS 可以版本化、锁定、同步、回滚；经验登记中有真实条目和效果证据。

## 第一阶段：先修正制度和契约一致性

### 1. 统一状态词汇

保留四个相互独立的维度：

- `execution.status`：`ready | draft | needs_review | blocked | failed`
- 领域判定：例如 ODD 的 `pass | fail | insufficient_evidence | conflict`
- 生命周期：`work_stage / model_role / run_status / review_status / decision_use`
- 数据状态：`exact | proxy | latest_only | missing | unverified`

`conflict`、`warning`、`proposed`、`ready_with_restriction` 不应再混入 `execution.status`。需要限制时使用 `ready + limitations` 或 `needs_review`。更新契约、测试用例和校验器，使非法状态直接失败，而不是只检查字段是否存在。

### 2. 明确 Skill 注册层级

将 56 个 Skill 分成四种角色：

- `primary`：可以作为一项任务的主入口；
- `support`：只能由主入口调用或建议，不能与主入口并列竞争；
- `reference`：提供知识、格式或实现资源，不参与路由；
- `meta`：维护 Skill、计划、审查和沉淀流程。

保留核心注册表也可以，但必须把全部 Skill 在 `skill-inventory.yaml` 中标注角色、是否可路由和所属主线。体检器应检查“未登记”与“明确标记为 reference/support”之间的区别。对 `supports` 数量不要只做机械的两个上限；建议改为“默认最多两个，超过时必须写明理由和调用顺序”，同时解决当前注册表注释与实际配置不一致的问题。

### 3. 建立真正的路由决策记录

不建议用简单关键词脚本代替模型判断。新增机器可校验的 `route-decision` 输出，至少包括：`primary_skill`、`support_skills`、`task_class`、`risk_level`、`trigger_evidence`、`excluded_skills`、`escalation`、`unresolved`。由 GPT-5.4+ 生成，由校验器检查唯一主入口、支持数量、别名、排除项和注册状态。这样可以验证模型是否按注册表路由，而不是假设注册表本身会自动执行路由。

## 第二阶段：建立版本、依赖和发布治理

### 1. 统一版本源

在 SharedSKILLS 增加发布清单，记录每个 Skill 的 canonical id、版本、来源提交、维护人、变更级别和兼容别名。所有当前 `unversioned` 的 Skill 先标为明确的初始版本，不需要一次性重写内容。

在 QuantSystem 后续建立 `skill-lock.yaml`，记录 SharedSKILLS 发布版本、各 Skill hash、项目覆盖、同步时间和允许的本地覆盖范围。同步脚本必须能报告：一致、落后、漂移、项目覆盖和源文件缺失。

### 2. 增加外部依赖清单

新增根级依赖清单，逐项说明外部路径或网络服务的提供项目、用途、是否必需、缺失处理（`blocked` 或 `warning`）、版本/校验方式和替代入口。优先处理 `docs/templates`、`docs/superpowers/plans`、`docs/retrospectives`、网络盘和项目专用脚本。Skill 不应让模型猜测外部路径，也不应把外部缺失误报为本地 Skill 损坏。

### 3. 清理发布边界

将预览图、历史 HTML、缓存、`__pycache__`、临时脚本和意外生成的文件从源码发布边界中分离。先建立保留/迁移清单，再删除；不因体检器报告就直接破坏可能有业务价值的历史产物。发布检查应拒绝把运行产物写回 Skill 源码目录。

## 第三阶段：把交付 Skill 接入冻结研究产物

对 `research-report-writer`、`frontend-report-page`、`frontend-ops-platform`、`docx`、`pptx`、`xlsx`、`pdf`、FOF 归因等交付能力增加统一的输入约束：必须接收 `artifact_id`、`review_status`、`as_of`、`source_manifest` 和 `run_manifest`。交付层只能重排和呈现已冻结的事实、判断、风险和行动；如果发现内容需要修改，必须退回研究层生成新 artifact。

这一步不要求所有交付 Skill 都加载全部投资契约，而是按职责分级：

- 投资结论型交付：必须消费 `evidence + artifact + execution`；
- 纯格式转换：只需声明输入文件、转换结果和格式验证证据；
- 视觉/图形能力：只负责表达，不产生投资判断，也不改变研究 artifact。

## 第四阶段：完成研究到组合的真实闭环

在 SharedSKILLS 侧先把接口定义稳定下来，在 QuantSystem 侧接入真实数据。最小闭环为：

```text
研究假设/预注册
→ 回测与稳健性证据
→ candidate/challenger 决议
→ 目标权重与组合约束
→ 实际持仓与成交
→ 成本、PnL、归因
→ 预测校准
→ 继续、降权、暂停或退役
```

新增或完善以下产物规范：

- research pre-registration：研究问题、机制、样本、PIT、成本、容量、主指标、稳健性、停止条件和多重检验说明；
- model card：模型角色、适用范围、失效条件、数据依赖、负责人和版本；
- portfolio decision artifact：目标权重、约束、风险预算、成本假设、批准人和有效期；
- outcome artifact：实际持仓、成交、已实现成本、PnL、归因、基准和预测校准；
- retirement record：暂停/退役触发、证据、决策人、替代模型和重新启用条件。

任何一个环节缺失，都只能输出 `research_only`、`draft` 或 `needs_review`，不能声称完成组合评估或模型退役。

## 第五阶段：用 GPT-5.4+ 做真实行为验收

现有 40 个案例应从“格式案例”升级为“模型行为回归集”。每个案例至少保存：模型版本、完整输入上下文、主路由、输出、结构化字段、自动校验结果、人工评分和失败分类。建议执行三类测试：

1. 路由测试：相邻 Skill 竞争、升级/降级、辅助 Skill 数量和别名；
2. 投资门槛测试：PIT 缺失、最新快照冒充历史、材料冲突、ODD 缺证据、得分边界、回测直接生产；
3. 反馈测试：研究结论没有权重、权重没有实际成交、成交没有成本/PnL/归因、复盘经验未经验证直接回写。

验收指标建议分为两层：

- 高风险门槛：不得出现伪造证据、把缺失当通过、把一次运行当生产、把 approved 当 live、覆盖原始预测或自动修改共享 Skill；
- 一般质量：主入口命中率、必填字段完整率、输出可追溯率、无效追问率和平均返工次数。

先用 GPT-5.4 作为最低能力基线，再用实际计划采用的更高模型做对照。若高模型表现好而 GPT-5.4 失败，应区分“模型能力差异”和“Skill 设计缺陷”，不能简单继续加规则。若两者都失败，优先修订契约、例外处理或路由边界。

## 第六阶段：让经验沉淀产生可度量收益

`experience-registry.yaml` 目前为空，应从最近已完成项目中选择真实案例录入，不要填充虚构经验。每条经验至少要有来源复盘、正反用例、目标位置、负责人、复核日期和效果指标。

经验效果至少观察一个周期：误路由率、漏门槛率、伪验证率、返工时间、人工追问次数、输出缺字段率或投资决策偏差。只有实际应用后证明有帮助，才保持 `applied`；造成误路由或额外负担时，进入 `deprecated` 或 `reverted`。`meta-retro` 应每季度审查新增规则是否带来净收益，而不是只统计新增条数。

## 需要先由投资负责人确认的制度参数

以下问题不能由 Skill 或模型自行决定，应在实施前形成制度参数文件：

1. ODD 一票否决项为 `insufficient_evidence` 时，采用保守暂不通过、形式满足还是证据加权；
2. 定量报告得分 65 的入池与风险等级解释；
3. 谁可以批准 `candidate → portfolio_input → live`，有效期和复核周期是多少；
4. 模型暂停、降权和退役的定量阈值与人工例外流程；
5. 哪些外部路径和网络源属于 SharedSKILLS 的正式运行依赖。

这些参数确定后才能把相应检查从 `needs_review` 提升为确定性门禁。

## 建议实施顺序与交付物

### P0：先完成一致性修复

- `shared-contracts/execution.yaml` 和所有测试案例的状态词汇统一；
- 注册表四类角色、全库标注和路由决策记录格式；
- 外部依赖清单和版本发布清单；
- 修复 `research-report-writer` BOM、`zm-skills-manager-local` 命名、处置台账未完成项；
- 静态校验器能阻断非法状态、无效引用和不一致 hash。

### P1：完成投资闭环和行为验收

- 冻结 artifact 到交付 Skill 的输入约束；
- GPT-5.4+ 真实行为回归及人工评分；
- research pre-registration、model card、portfolio decision、outcome、retirement 五类产物；
- 用真实项目填充经验登记。

### P2：优化结构和长期维护

- 仅对高频、超长或职责混杂的 Skill 做渐进式拆分；
- 合并重复的前端/架构图入口，拆分 `writing-skills` 的设计、审查和压力测试职责；
- 建立季度 meta-retro、版本发布和回归复测节奏。

## 本轮完成的验收门槛

只有同时满足以下条件，才可以称为“下一轮改进完成”：

- 静态检查、契约检查和版本锁定均通过；
- GPT-5.4 基线在所有高风险行为案例中零关键违规；
- 主路由和交付产物在重复运行中达到预设稳定性；
- 至少一个真实量化项目完成研究—组合—结果反馈闭环；
- 至少三条真实经验完成 `applied`，并有一条被证明需要 `deprecated` 或 `reverted` 的反例，或有充分证据说明尚无此类案例；
- 所有未完成事项仍明确标为 `needs_review`、`blocked` 或后续批次，不能用“文件已生成”替代行为验收。

在这些门槛达成前，SharedSKILLS 的准确定位是：**核心投资研究治理已基本成形，正在进行运行时和投资结果层验收。**
